import styled from 'styled-components';
import { Card } from '../../components/Card';

export const OrderWrapper = styled(Card)``;

export const DeliveryWrapper = styled(Card)`
  display: flex;
  flex: 1;
  flex-direction: column;
  margin-top: 0;
`;

export const PaymentWrapper = styled(Card)`
  display: flex;
  flex: 1;
  flex-direction: column;
  margin-top: 0;
`;
